[
build
]
     command = "npm run build"
     publish = ".next"

   [[
plugins
]]
     package = "@netlify/plugin-nextjs"

   [
functions
]
     # 将超时时间设置为 30 秒
     timeout = 30

   [[
redirects
]]
     from = "/*"
     to = "/index.html"
     status = 200
   